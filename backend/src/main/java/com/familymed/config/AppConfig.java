package com.familymed.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Configuration placeholder; beans defined in SecurityConfig to avoid duplicates
}
